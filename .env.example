# Copy to .env and fill in. .env is gitignored; never commit real keys.

# OpenAI key used by the LiteLLM gateway for local dev (chat + embeddings).
# Rotate/revoke any key that has ever been shared or committed.
OPENAI_API_KEY=